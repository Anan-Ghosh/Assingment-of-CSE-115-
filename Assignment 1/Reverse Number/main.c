#include <stdio.h>
#include <stdlib.h>
#include<math.h>
int main()
{
    int a,b;

    printf("Enter the value of a:");
    scanf("%d",&a);

    b=(-(a));
    printf("Reverse the number is %d",b);

    return 0;
}
